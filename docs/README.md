\# Car Dealership Management System 🚗



این پروژه مربوط به جلسه اول درس پروژه عملی است و هدف آن طراحی اولیه سیستم مدیریت خرید و فروش خودرو می‌باشد.



---



&nbsp;هدف پروژه

ایجاد بستری برای:

\- ثبت اطلاعات خودروها

\- ثبت و مدیریت مشتریان

\- بررسی درخواست‌های خرید

\- مدیریت توسط ادمین‌ها



---



&nbsp;مدل پایگاه داده (ERD)

در این سیستم جداول زیر در نظر گرفته شده است:

\- Users (مدیران و مشتریان)

\- Cars (خودروها)

\- PurchaseRequests (درخواست‌های خرید)

\- CarImages (تصاویر هر خودرو)



ارتباط‌ها:

\- هر مشتری می‌تواند چند درخواست خرید ثبت کند.

\- هر خودرو می‌تواند چند تصویر داشته باشد.

\- هر درخواست متعلق به یک مشتری و یک خودرو است.



---



&nbsp;فناوری‌های استفاده شده

| بخش  فناوری |

|------|----------|

| Backend | Node.js + Express |

| Frontend | HTML, CSS |

| Database (آینده) | MySQL |

| Version Control | Git \& GitHub |


---


💡جلسه دوم – پیاده‌سازی پایگاه داده و ساختار اولیه  

هدف جلسه
در این جلسه هدف آماده‌سازی کامل دیتابیس و ساختار اولیه‌ی پروژه است.  
شامل سه بخش اصلی:

 1.  ایجاد دیتابیس MySQL و جداول طبق ERD
  
 2. تنظیم بک‌اند اولیه با Node.js و Express  

 3. طراحی فرانت‌اند ساده (HTML/CSS) برای نمایش لیست خودروها  

---

ساخت پایگاه داده (Database)

دیتابیس:
نام دیتابیس: `car_dealership`

جداول اصلی:
- **Users** → اطلاعات کاربران (مدیر، مشتری)
- **Cars** → اطلاعات خودروها  
- **PurchaseRequests** → درخواست‌های خرید  
- **CarImages** → نگهداری تصاویر خودرو  

فایل مربوطه: `database.sql`  
این فایل شامل تمام اسکریپت‌های SQL برای ایجاد جداول فوق است.

---

جلسه سوم: پیاده‌سازی ثبت‌نام، ورود و اتصال فرانت‌اند به بک‌اند

هدف:
ایجاد سیستم احراز هویت (ثبت‌نام و ورود کاربران) و اتصال کامل بین front و backend.

🔹 کارهای انجام‌شده:

ایجاد APIهای زیر:

POST /api/auth/signup → ثبت‌نام کاربر جدید

POST /api/auth/login → ورود کاربر با رمز هش‌شده

POST /api/auth/logout → خروج کاربر

استفاده از کتابخانه‌های زیر:

bcryptjs برای هش کردن رمز عبور

jsonwebtoken برای مدیریت نشست (JWT)

cookie-parser برای مدیریت کوکی‌ها

ایجاد فایل‌های:

/routes/authRoutes.js

/controllers/authController.js

ساخت صفحات فرانت‌اند:

login.html برای ورود

signup.html برای ثبت‌نام

cars.html برای نمایش لیست خودروها به همراه تصاویر

اتصال کامل بین سرور Node.js و دیتابیس MySQL

اضافه شدن جدول carimages و نمایش تصاویر مرتبط با خودروها

تست دستی ثبت‌نام و ورود یک مدیر و مشتری با موفقیت انجام شد ✅


---

📘 جلسه چهارم – پیاده‌سازی نقش‌ها و داشبورد کاربران

در این جلسه هدف اصلی تفکیک نقش‌ها (Role-Based Access)، ایجاد داشبورد مخصوص هر نقش، و افزودن قابلیت خروج بود.
همچنین باید مسیرهای محافظت‌شده (Protected Routes) در Backend ایجاد و تست می‌شد.

🎯 اهداف جلسه چهار

پیاده‌سازی سیستم Role-Based Access در Backend

ایجاد داشبورد مدیر (Admin Dashboard)

ایجاد داشبورد مشتری (Customer Dashboard)

ساخت دکمه خروج (Logout)

اتصال کامل داشبوردها به API

تست دسترسی بر اساس نقش

آپدیت مستندات پروژه

🟦 ۱. پیاده‌سازی Role-Based Access در Backend

در مسیر:
backend/middleware/auth.js

سه میدل‌ور ساخته شد:

✔ authRequired

بررسی می‌کند کاربر لاگین کرده و توکن معتبر دارد.

✔ adminOnly

دسترسی فقط برای مدیر مجاز است.

✔ customerOnly

دسترسی فقط برای مشتری مجاز است.

این میدل‌ورها نقش‌ها را از داخل JWT بررسی می‌کنند.

🟩 ۲. API مربوط به داشبورد نقش‌ها
📌 داشبورد مدیر
GET /api/admin/dashboard


فقط مدیر می‌تواند به این مسیر دسترسی داشته باشد و لیست خودروها را مشاهده کند.

📌 داشبورد مشتری
GET /api/customer/dashboard


مشتری تنها اطلاعات کاربری خود را مشاهده می‌کند.

✔ تست انجام شد

مدیر فقط به مسیر مدیر دسترسی دارد
و مشتری فقط به مسیر مشتری.

🟧 ۳. ایجاد داشبورد فرانت‌اند

در پوشه frontend فایل‌های زیر ساخته شد:

✔ admin-dashboard.html

نمایش لیست خودروها برای مدیر

دکمه خروج

✔ customer-dashboard.html

نمایش اطلاعات کاربر برای مشتری

دکمه خروج

هر دو صفحه با fetch به APIهای داشبورد متصل شدند.

🟫 ۴. پیاده‌سازی Logout

در frontend دا‌کمه خروج اضافه شد که کوکی JWT را حذف می‌کند:

POST /api/auth/logout


پس از خروج → کاربر دوباره به login.html هدایت می‌شود.

🟪 ۵. اصلاح مسیر بعد از Login

در login.html مسیر هدایت بر اساس نقش تنظیم شد:

اگر مدیر باشد → وارد admin-dashboard.html

اگر مشتری باشد → وارد customer-dashboard.html

این بخش نقش‌ها را به‌صورت کامل در Frontend تفکیک می‌کند.

🟨 ۶. تست‌های انجام‌شده
✔ تست مدیر:

dashboard → فعال

مسیر مشتری → مسدود

✔ تست مشتری:

dashboard → فعال

مسیر مدیر → مسدود

✔ تست بدون لاگین:

هر دو مسیر dashboard ممنوع می‌شود.

🟦 ۷. نتیجه جلسه چهار

✔ سیستم نقش‌ها کامل شد
✔ داشبورد مدیر کامل شد
✔ داشبورد مشتری کامل شد
✔ مسیرهای محافظت‌شده اضافه شد
✔ Logout کامل شد
✔ تست همه نقش‌ها موفقیت‌آمیز بود
✔ ساختار فرانت‌اند و بک‌اند هماهنگ شد

---

🚗 Car Dealership Management System — جلسه ۵

در این جلسه قابلیت ثبت و مدیریت خودروها توسط مدیر پیاده‌سازی شد.
این بخش یکی از مهم‌ترین قسمت‌های پروژه است و شامل فرانت‌اند، بک‌اند، دیتابیس و آپلود عکس می‌شود.

📌 اهداف جلسه ۵

ساخت فرم ثبت خودرو در فرانت‌اند

افزودن فیلدهای لازم:

brand

model

year

price

آپلود عکس خودرو

ساخت API مخصوص مدیر برای ثبت خودرو در دیتابیس

ایجاد قابلیت مشاهده لیست خودروها در پنل ادمین

امکان حذف، ویرایش، و افزودن چند عکس برای هر خودرو

تست نهایی: ثبت یک خودرو و بررسی نمایش آن در تمام صفحات

آپدیت README پروژه

🧱 ساختار بک‌اند (جلسه ۵)
📂 فایل مهم در بک‌اند:
backend/
  routes/
    adminCarRoutes.js   ← افزودن/ویرایش/حذف خودرو + مدیریت عکس‌ها
  server.js             ← ارسال لیست خودروها + سرو استاتیک عکس‌ها

✅ امکانات اضافه‌شده به بک‌اند

استفاده از multer برای آپلود عکس

ذخیره اطلاعات خودرو در جدول cars

ذخیره مسیر عکس‌ها در جدول carimages

APIهای جدید:

Route	Method	توضیح
/api/admin/cars	POST	افزودن خودرو + آپلود ۱ عکس
/api/admin/cars/:id	DELETE	حذف خودرو
/api/admin/cars/:id	PUT	ویرایش خودرو
/api/admin/cars/:id/image	POST	افزودن عکس بیشتر
/api/admin/cars/image/:imageId	DELETE	حذف عکس
/api/admin/dashboard	GET	دریافت لیست کامل همراه عکس‌ها
📌 اصلاح اصلی server.js

برای رفع مشکل نمایش عکس‌ها:

app.use('/images', express.static(path.join(__dirname, '../frontend/images')));

🎨 ساختار فرانت‌اند (جلسه ۵)
📄 فایل‌های اصلاح‌شده:
frontend/
  admin-dashboard.html   ← مدیریت خودرو + چندعکسی + حذف عکس
  customer-dashboard.html ← نمایش خودرو + عکس‌ها
  cars.html                ← لیست عمومی خودروها

قابلیت‌های اضافه‌شده:

فرم ثبت خودرو جدید + آپلود عکس

نمایش لیست خودروها

نمایش چند عکس برای هر خودرو

دکمه حذف عکس برای هر عکس

دکمه افزودن عکس جدید

ویرایش اطلاعات خودرو

حذف خودرو

🗄️ اصلاح دیتابیس

فیلد price در جدول cars → تبدیل به:

BIGINT UNSIGNED


برای جلوگیری از خراب شدن قیمت‌های بزرگ مثل 990,000,000.

🧪 تست نهایی (جلسه ۵)

✓ یک خودرو جدید ثبت شد
✓ عکس آپلود شد و مسیر صحیح ذخیره شد
✓ خودرو در داشبورد مدیر نمایش داده شد
✓ امکان حذف و افزودن عکس تست شد
✓ خودرو در customer-dashboard نمایش داده شد
✓ خودرو در cars.html نمایش داده شد
✓ مسیر /images/... به درستی کار کرد

جلسه ۵ با موفقیت کامل انجام شد ✔✔✔
